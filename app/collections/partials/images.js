import { title } from './title.js'
import { image } from './image.js'

export const images = {
    name: 'images',
    label: 'Images',
    widget: 'list',
    i18n: true,
    fields: [
        title,
        image
    ]
}