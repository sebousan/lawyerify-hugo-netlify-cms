import { title } from '../partials/title.js'
import { image } from '../partials/image.js'

export const images = {
    name: 'images',
    label: 'Images',
    widget: 'list',
    i18n: true,
    fields: [
        title,
        image
    ]
}